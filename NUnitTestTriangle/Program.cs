using NUnitDemonstration;

var triangle = new Triangle();

int firstAngle = 60;
int secondAngle = 60;
int thirdAngle = 60;

string outputtxt = triangle.ValidTriangle(firstAngle,secondAngle,thirdAngle);

Console.WriteLine(outputtxt);